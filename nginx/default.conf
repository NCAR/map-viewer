server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://map-viewer-backend:8000/;
        # proxy_pass http://backend:8000/; # Local testing
    }
}

